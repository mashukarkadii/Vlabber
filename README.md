# Vlabber - приложение для обмена текстовыми сообщениями

Написан на Python, АПИ тоже.

Сообщения шифруются с помощью AES. Ключ для расшифровки создается из строки в переменной self.CRYPT_KEY в файле vlabber.py

